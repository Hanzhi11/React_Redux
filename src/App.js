// run app using npm start run, run json server using npm run start:server

import UsersList from "./components/UsersList"

export default function App() {
    return (
        <div className="container mx-auto">
            <UsersList />
        </div>
    )
}